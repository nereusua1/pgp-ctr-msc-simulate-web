# 报文模板通过发布版本生效

报文模板的日常编辑不应立即影响正在运行的任务。系统将已发布版本与待发布修改分离：已启用任务始终使用最近的已发布版本，管理员完成预览和校验后再发布新版本，以换取运行稳定性和可审计性，而不是让保存操作直接生效。

## Consequences

后端需要保存报文模板版本或等价快照；每个执行批次启动时锁定模板版本，正在执行的批次不受后续发布影响；执行记录需要展示实际使用的版本号；发布前校验失败时不得替换当前已发布版本。
