# 报文模拟

面向消息云联调场景，管理报文配置、模拟任务及其执行证据。

## 用户与权限

**管理员**：
负责系统配置、任务维护、任务执行和异常处理的核心用户。

**任务操作员**：
可以查看系统信息并对已有任务发起历史补跑，但不能维护配置或立即执行任务的受限用户。
_Avoid_: 普通用户、操作员

## 配置

**消息云组件**：
模拟任务唯一允许使用的投递目标，包含消息云实例连接信息、发送组与 Topic。
_Avoid_: 消息组件、MQ 实例、投递通道

**报文模板**：
定义报文内容、数据绑定、生成规则与消息云投递目标的可发布配置。
_Avoid_: 报文

**文件规则模板**：
绑定数据源、数据项及完整要素集合，定义文件结构识别与时间替换规则的可复用配置。

**已发布版本**：
报文模板当前对已启用任务生效的稳定版本。

**待发布修改**：
报文模板已保存但尚未影响已启用任务的编辑内容。

**生成预览**：
管理员使用样例数据和指定业务时间检查报文模板生成结果的发布前验证。

**发布校验**：
发布报文模板时执行的完整有效性检查；校验失败的草稿不能成为已发布版本。

## 任务执行

**自动调度状态**：
表示任务是否按照调度规则自动创建执行批次；停用自动调度不等于禁止手工执行。

**自动执行有效期**：
模拟任务允许自动调度的可选北京时间日期范围，包含开始日期和结束日期；未配置表示长期有效。该范围不限制立即执行和历史补跑。
_Avoid_: 业务时间范围、历史补跑范围

**手工执行可用性**：
表示任务当前是否满足立即执行或历史补跑的必要条件，与自动调度状态相互独立。

**执行批次**：
一次任务执行及其全部投递明细的集合，启动时锁定所使用的报文模板版本。
_Avoid_: Execution

**立即执行**：
管理员使用当前业务时间手工启动已有任务的一次执行。

**历史补跑**：
管理员或任务操作员使用指定的过去业务时间手工执行整个任务，不改变任务原有调度配置，也不单独重试某条失败投递明细。未来时间不能用于历史补跑。
_Avoid_: 补跑、重跑
